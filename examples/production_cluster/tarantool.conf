;
; examples/production_cluster/tarantool.conf
; Asterisk Tarantool 3.x Connection Pool Configuration
;

[general]

[tnt1]
host = 127.0.0.1
port = 3301
user = asterisk
secret = secret123
pool_size = 8
timeout_ms = 500
